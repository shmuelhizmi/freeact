import { createApiServerInterface } from "freeact";
import { APIEventsMap, API } from "../types";

export const api = createApiServerInterface<APIEventsMap, API>(({ emit }) => {
  return {
    alert(message: string) {
      emit("alert", message);
    },
  };
});
